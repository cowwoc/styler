/**
 * Line length formatting rule with context-aware wrapping.
 * <p>
 * This package provides semantic-aware line wrapping that uses the AST to determine
 * optimal break points based on the syntactic context (method chains, arguments,
 * binary expressions, etc.).
 */
package io.github.cowwoc.styler.formatter.linelength;
