# Test Document 1A: Git Workflow

**Rule 1**: Always create a backup branch before rebasing.

**Rule 2**: Verify changes after rebase using git diff.

**Rule 3**: Never force-push to main branch.

**Rule 4**: Cleanup backup branches after successful merge.
