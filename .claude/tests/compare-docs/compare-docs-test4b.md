# Rebase Best Practices

Create a backup branch before you perform the rebase operation.

Verify all changes using git diff after you complete the rebase.

Delete the backup branch once verification is successful.
