# Safe Rebase Guidelines

Before rebasing, create a backup branch to preserve your current state.

After completing the rebase, verify all changes are present using git diff.

Once verification succeeds, you can safely delete the backup branch.
