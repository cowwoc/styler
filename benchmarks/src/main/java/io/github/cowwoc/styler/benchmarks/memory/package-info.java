/**
 * Benchmarks for memory usage and efficiency.
 *
 * Validates the performance claim of <=512MB heap usage per 1000 files processed. Uses GC profilers
 * and SingleShotTime measurements to capture peak memory usage accurately.
 */
package io.github.cowwoc.styler.benchmarks.memory;
