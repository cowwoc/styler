/**
 * Benchmarks for parallel processing scalability.
 *
 * Validates linear scaling behavior across 1 to 32 processor cores. Uses parameterized benchmarks
 * to measure throughput at different thread counts and compute speedup ratios.
 */
package io.github.cowwoc.styler.benchmarks.scalability;
